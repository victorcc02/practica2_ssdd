<!DOCTYPE html>
<html>
    <head>
        <title>Rutinas</title>
        <link rel="stylesheet" href="/css/styleRoutines.css">
        <script>
            function goBackwards(){
                window.location.href = "/FrontPage?id={{userId}}";
            }
            function goToCreate(){
                window.location.href = "/routines/createRoutine?id={{userId}}";
            }
            function tapar() {
                OtroArchivo = new OtroArchivo();
                if (.miMapa.isEmpty()) { // Verificas si el mapa está vacío
                    document.getElementById("conElemntos").style.display = "none";
                }else{
                    document.getElementById("sinElementos").style.display = "none";
                }
            }
                 // Creas una instancia de la clase que contiene el mapa


        </script>
    </head>
    <body>
        <div class="goBackButtonContainer">
            <button type="button" class="goBackButton" onclick="goBackwards()">
                <img src="/imagenes/goBack.jpg" alt="flechaPaginaAnterior">
            </button>
        </div>
        <br><br>
        <div class="goToCreateButtonContainer">
            <button type="button" class="goToCreateButton" onclick="goToCreate()">Crear rutina</button>
        </div>
        {{^routines}}
            <p class="noRoutines">Todavia no se han agregado rutinas</p>
        {{/routines}}
        <div class="selectContainer">
        {{#routines}}
            {{^firstRoutine}}
                    <select class="select">
            {{/firstRoutine}}
                        <option>
                            {{routineName}}
                        </option>
            {{^lastRoutine}}
            {{/lastRoutine}}
                    </select>
        {{/routines}}
        </div>
    </body>
</html>