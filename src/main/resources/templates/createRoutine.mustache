<!DOCTYPE>
<html>
    <head>
        <title>Crea una rutina</title>
        <script>
            function goBackwards(){
                window.location.href = "/routines?id={{userId}}";
            }
        </script>
    </head>
    <body>
        <div class="goBackButtonContainer">
            <button type="button" class="goBackButton" onclick="goBackwards()">
                <img src="/imagenes/goBack.jpg" alt="flechaPaginaAnterior">
            </button>
        </div>
        <form action="/routines/createRoutine?={{userId}}" method="post">
            <label for="routineName" class="title">Nombre de la rutina</label><br>
            <input id="routineName" class="field" name="name" type="text" minlength="1" maxlength="15"
                   placeholder="Inserta el nombre...">
            <br><br>
            <label for="intensity" class="title">Intensidad</label><br>
            <select name="intensity"  class="select" id="intensity">
                <option>Baja</option>
                <option>Moderada</option>
                <option>Alta</option>
            </select>
            <br><br>
            <label for="duration" class="title">Duracion</label><br>
            <input id="duration" name="duration" type="number" inputmode="numeric" step="1"
               min="1" max="600" class="numeric" placeholder="Duracion (minutos)">
            <br><br>
            <label for="exercises" class="title">Ejercicios</label><br>
            <textarea name="exercises" id="exercises" rows="10" cols="50">
                Ejemplo empuje:
                Pecho -> Press banca (4 x 12)
                         Press banca inclinado (4 x 12)
                         Cruces polea alta(4 x 10)
                Triceps -> Fondos (4 x 10)
                           Extesiones polea (4 x 12)
                           Rompecraneos (3 x 10)
                Hombro -> Press militar (3 x 10)
                          Elevaciones laterales (4 x 10)
                Cardio -> Correr (20')
            </textarea>
            <label for="goal" class="title">Objetivo</label><br>
            <select name="goal"  class="select" id="goal">
                <option>Bajar de peso</option>
                <option>Mantenerse</option>
                <option>Subir de peso</option>
            </select>
        </form>
    </body>
</html>